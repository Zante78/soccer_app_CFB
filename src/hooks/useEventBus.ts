/**
 * Hook zum einfachen Verwenden des Event Bus in React Komponenten.
 * 
 * @param event - Der Event-Name auf den gehört werden soll
 * @param callback - Die Callback-Funktion die ausgeführt wird
 * 
 * @example
 * ```tsx
 * useEventBus('player:updated', (player) => {
 *   console.log('Player updated:', player);
 * });
 * ```
 */
// ... Rest des Codes bleibt gleich ...